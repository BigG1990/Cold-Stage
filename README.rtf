This code is written in Python 3.7, compatibility issues may be a concern with other versions.

The code use some fixed parameters that might have better values. 

Depends on the lighting produced by the LED and exposure settings, parameters in the code need to be adjusted. Including these two parameters: 1. the brightness_probe_size; 2. the delta_significance_threshold.

An example of image data set is in WW20 folder.
